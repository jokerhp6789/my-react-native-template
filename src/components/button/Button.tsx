import React from 'react';
import {View} from 'react-native/Libraries/Components/View/View';

export interface IButtonProps {
  [key: string]: any;
}

const Button: React.FC<IButtonProps> = ({id}) => {
  return <View />;
};

export default Button;
